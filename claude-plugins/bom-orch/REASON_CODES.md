# Reason codes

<!-- generated — do not edit. Run `npm run contract:snapshot` after changing src/reason-codes.mjs or src/reason-text.mjs. -->

Every failure this server reports carries a `reasonCode`. The coarse `stopReason` beside it is
derived from the code, never chosen separately, so one code always ends a run the same way.
This table is generated from `src/reason-codes.mjs` (the vocabulary) and `src/reason-text.mjs` (the wording): 408 codes.

The `Message` and `Recovery` columns are the templates this server renders, so they keep their
`{placeholders}`; a real result fills them in with the values from that run.

| Reason code | stopReason | Message | Recovery |
| --- | --- | --- | --- |
| `apply_applied_unverified` | `infrastructure_failed` | Git reported a completed three-way write in {path}, but the post-apply check could not run; mapped paths may match their pre-write snapshots or may have changed again, and the index is unproven: {detail} | Inspect git status, git diff, and git diff --cached in that repository, then review the working tree and index; do not assume the patch is currently applied or staged |
| `apply_artifact_mismatch` | `infrastructure_failed` | The bytes at {path} do not hash to the digest this run recorded for its patch | Start a new run rather than applying bytes the run cannot vouch for |
| `apply_baseline_moved` | `blocked` | The patch does not apply to {path} as it stands; either the temporary baseline merge reached no clean result or the real three-way write failed without a measured patch-path or porcelain change | Inspect git status --untracked-files=all for an untracked file occupying a path this patch creates; move or preserve it if present, otherwise start a new run against the repository as it is now |
| `apply_baseline_pruned` | `blocked` | Patch preimage material needed for a three-way merge is no longer available in {path}; that may be a referenced blob or the baseline index entry a pure mode-only change needs, and git can discard unreachable objects before this server's retained patch expires | Start a new run against the repository as it is now, or apply the patch by hand with git apply and resolve the differences yourself |
| `apply_git_failed` | `infrastructure_failed` | A required Git-related preparation or operation could not complete: {detail} | Fix the reported Git or filesystem problem, then call orch_apply again |
| `apply_head_moved` | `blocked` | The target repository HEAD changed after the pre-write checks, so this call did not apply the patch to {path} | Review the new HEAD and its committed .bom-orch.json, then call orch_apply again only if that policy still approves the patch |
| `apply_patch_empty` | `blocked` | The patch this run left is empty, so there is nothing to apply and {path} was not touched | Call orch_status with this run_id: an empty patch means the delegate changed no file |
| `apply_patch_missing` | `blocked` | Run {runId} has no patch to apply on this state root: it left no representative patch, or the cleanup has already reclaimed it | Call orch_status with this run_id to see which candidate patches are still on disk |
| `apply_patch_unreadable` | `infrastructure_failed` | Something is at {path} but it is not a readable patch file | Look at what occupies that path, then start a new run rather than applying whatever is there |
| `apply_run_not_found` | `blocked` | No run named {runId} is on this state root, so there is nothing to apply | Call orch_status with no arguments to list the recent runs |
| `apply_project_unknown` | `blocked` | The records of run {runId} do not name the repository it was started in, so there is no repository to apply its patch to | Locate the repository that run was started in from your own records, then apply the retained representative patch there with git apply; orch_apply cannot infer the target |
| `apply_project_unusable` | `blocked` | The repository this run was started in, {path}, is missing, is not a git repository, or has no commit to apply against | Restore that repository, or apply the patch yourself with git apply where you want the changes |
| `apply_rollback_incomplete` | `infrastructure_failed` | The apply into {path} stopped after writing and {count} patch paths now differ from the pre-write snapshot or could not be inspected safely; the current tree was left untouched and the mapped backup is kept at {backup}: {detail} | Copy backup/manifest.json and any bytes you need out of {backup} before anything else, then inspect git status and repair the repository by hand; nothing on this server reclaims that directory |
| `apply_run_unreadable` | `infrastructure_failed` | The records of that run could not be read, so its patch cannot be checked against the run it came from | Call orch_status with no arguments to list the recent runs |
| `apply_scope_refused` | `policy_failure` | The patch has scope-policy findings that are not approved by scope.allow at the target repository's current HEAD, so nothing was applied | Review scope.reasons; if every finding is intended and waivable, commit matching scope.allow entries and retry, otherwise start a new run or apply the patch manually |
| `apply_three_way_conflicted` | `blocked` | Merging this patch into {path} ends in conflicts; that was measured in a temporary index, so nothing was written to your files | Apply the patch by hand with git apply --3way and resolve the conflicts, or start a new run against the repository as it is now |
| `apply_verification_failed` | `infrastructure_failed` | The final post-write check for {path} disagreed or could not run, so the working tree or index cannot be vouched for: {detail} | Before changing anything else, inspect git status, git diff, and git diff --cached in that repository; preserve any work you need, then repair the reported working-tree or index state by hand |
| `apply_worktree_dirty` | `blocked` | This patch needs a three-way merge, but {count} tracked changes in {path} either touch a patch path that does not match the index or are already staged; Git refuses the former, and the latter prevents orch_apply from predicting and verifying the result against a HEAD-seeded index | Commit or stash those changes, then call orch_apply again |
| `apply_write_blocked` | `blocked` | git could not safely finish this patch in {path}: an unsafe path shape blocked the write, or a failed direct write left no measured patch-path or porcelain change | Inspect git status and the paths this patch names, clear any obstruction, then call orch_apply again or start a new run against the repository as it is now |
| `artifact_allocation_checkpoint_failed` | `infrastructure_failed` | The run manifest could not durably record the attempt allocation, so the attempt never started | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_attempt_input_invalid` | `infrastructure_failed` | The attempt record was requested with input that failed validation or named an unknown attempt | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_attempt_record_invalid` | `infrastructure_failed` | The attempt record failed normalization, so it was never written | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_attempt_too_large` | `infrastructure_failed` | The attempt record exceeded the artifact size limit of {limit} bytes | Retry with a narrower task so the attempt record stays under {limit} bytes |
| `artifact_attempt_write_failed` | `infrastructure_failed` | The immutable attempt record could not be written or failed its byte verification | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_base_directory_not_private` | `blocked` | The artifact base directory {path} is not owner-only | Restrict that directory to its owner, then retry the run |
| `artifact_candidate_checkpoint_failed` | `infrastructure_failed` | A candidate checkpoint could not be recorded in the run manifest | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_candidate_input_invalid` | `infrastructure_failed` | The candidate patch was requested with input that failed validation | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_candidate_patch_mismatch` | `infrastructure_failed` | The candidate patch bytes did not match the digest sealed for that attempt | Start a new run; the state this run built can no longer be trusted |
| `artifact_candidate_path_mismatch` | `infrastructure_failed` | A candidate lane wrote its patch somewhere other than the path the frozen budget reserved | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_candidate_write_failed` | `infrastructure_failed` | The candidate patch could not be stored or failed its reference check | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_collision_input_invalid` | `infrastructure_failed` | The namespace collision probe was called with a malformed input object | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_collision_inspection_failed` | `infrastructure_failed` | The namespace collision probe could not read the artifact root {path} | Check that the artifact root exists and is readable by this user, then retry |
| `artifact_create_once_publish_failed` | `infrastructure_failed` | Publishing the artifact from its temporary file to its final path failed | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_destination_exists_or_publish_failed` | `infrastructure_failed` | The final artifact path was already taken, or publishing to it failed | Start a new run; the state this run built can no longer be trusted |
| `artifact_directory_sync_failed` | `infrastructure_failed` | The artifact directory could not be flushed to disk, so the write cannot be proven durable | Check that the state root is writable and the disk has free space, then retry |
| `artifact_evidence_input_invalid` | `infrastructure_failed` | The evidence record was requested with input that failed validation | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_evidence_record_invalid` | `infrastructure_failed` | The evidence record failed normalization, so it was never written | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_final_inspection_failed` | `infrastructure_failed` | The published artifact could not be inspected afterwards, so the write stays unproven | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_identity_invalid` | `infrastructure_failed` | The artifact path could not be derived from the frozen run identity | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_init_lock_collision` | `infrastructure_failed` | Another initializer already owns the artifact namespace for run {runId} | Wait for the other run to finish, then start a new run |
| `artifact_initial_manifest_invalid` | `infrastructure_failed` | The initial run manifest did not normalize, so the run namespace was never created | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_initial_manifest_too_large` | `infrastructure_failed` | The initial run manifest exceeded the manifest size limit of {limit} bytes | Retry with a narrower task so the manifest stays under {limit} bytes |
| `artifact_initialization_dependency_failed` | `infrastructure_failed` | A filesystem dependency the artifact store needs was missing or refused | Check that the state root is writable and the disk has free space, then retry |
| `artifact_initialization_failed` | `infrastructure_failed` | The artifact store failed to initialize and left no evidence of a crash | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_initialization_interrupted` | `infrastructure_failed` | Artifact store initialization crashed midway and left partial state behind | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_inspection_input_invalid` | `infrastructure_failed` | The artifact reference inspection was called with a malformed input object | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_manifest_authority_mismatch` | `infrastructure_failed` | The run manifest on disk did not match the authority frozen for this run | Start a new run; the state this run built can no longer be trusted |
| `artifact_manifest_checkpoint_failed` | `infrastructure_failed` | A run manifest checkpoint failed, so the artifact store lost its authority | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_manifest_event_invalid` | `infrastructure_failed` | A run manifest checkpoint event failed validation | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_manifest_event_path_invalid` | `infrastructure_failed` | A run manifest event named the path {path}, which is outside this run namespace | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_manifest_event_payload_mismatch` | `infrastructure_failed` | A replayed manifest event arrived with a different payload than the one already applied | Start a new run; the state this run built can no longer be trusted |
| `artifact_manifest_replace_failed` | `infrastructure_failed` | The run manifest file could not be replaced atomically | Check that the state root is writable and the disk has free space, then retry |
| `artifact_manifest_transition_invalid` | `infrastructure_failed` | A manifest event would move the run into a state the manifest forbids | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_namespace_collision` | `blocked` | An artifact namespace already exists for run {runId}, and existing bytes are never adopted | Start a new run; the existing bytes were left untouched |
| `artifact_partial_write_requires_recovery` | `infrastructure_failed` | A partial artifact file is still on disk, so the write cannot be retried until recovery runs | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_patch_write_failed` | `infrastructure_failed` | The candidate patch bytes could not be collected, or they did not match the delta they must match | Start a new run; the state this run built can no longer be trusted |
| `artifact_path_budget_exceeded` | `blocked` | The artifact paths for this run would exceed the {limit} character path budget, so the run was refused before any credit was spent | Move BOM_ORCH_HOME to a shorter absolute path, then retry |
| `artifact_paths_invalid` | `infrastructure_failed` | The artifact path budget was checked with a non-canonical state root, an invalid run identity, or an unsupported candidate count | Set BOM_ORCH_HOME to an existing absolute path and ask for one or two candidates |
| `artifact_pending_final_mismatch` | `infrastructure_failed` | The published artifact did not match the immutable reference recorded for it | Start a new run; the state this run built can no longer be trusted |
| `artifact_pending_replay_mismatch` | `infrastructure_failed` | A pending artifact reservation named a different path than the write that replayed it | Start a new run; the state this run built can no longer be trusted |
| `artifact_permission_verification_failed` | `infrastructure_failed` | The artifact path {path} could not be proven owner-only | Check the owner and the permission bits under the artifact root, then retry |
| `artifact_plan_record_invalid` | `infrastructure_failed` | The plan this run made could not be recorded, so the run kept no readable copy of it | The run itself is unaffected; report this if orch_status keeps showing no plan for finished runs |
| `artifact_replay_mismatch` | `infrastructure_failed` | A replayed write found bytes on disk that do not match what the run manifest recorded | Start a new run; the state this run built can no longer be trusted |
| `artifact_reservation_lost` | `infrastructure_failed` | The pending write reservation disappeared from the run manifest mid-write | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_root_not_canonical` | `blocked` | The artifact root {path} could not be resolved to a canonical directory | Create BOM_ORCH_HOME as a real absolute directory with no symlink loop, then retry |
| `artifact_root_overlaps_project` | `blocked` | The artifact root lives inside the target repository, which would make artifacts part of the diff | Move BOM_ORCH_HOME to a path outside the target repository, then retry |
| `artifact_selection_checkpoint_failed` | `infrastructure_failed` | A usage or selection checkpoint could not be recorded in the run manifest | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_store_authority_lost` | `infrastructure_failed` | A manifest checkpoint settled in an unknown state, so the artifact store can no longer be trusted | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_store_handle_invalid` | `infrastructure_failed` | The artifact store handle passed in was not one this server created | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_store_initialization_failed` | `infrastructure_failed` | The artifact store refused to initialize and reported no reason of its own | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_store_input_invalid` | `infrastructure_failed` | The artifact store was created with a malformed input object | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_store_invalid` | `infrastructure_failed` | The artifact store failed its shape or authority check right after it was created | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_store_poisoned` | `infrastructure_failed` | An earlier fault poisoned the artifact store, so every later write is refused | Start a new run; the state this run built can no longer be trusted |
| `artifact_temp_inspection_failed` | `infrastructure_failed` | The temporary artifact file could not be inspected | Check that the state root is writable and the disk has free space, then retry |
| `artifact_temp_mismatch` | `infrastructure_failed` | The temporary artifact path is not the regular file this server created | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_temp_name_failed` | `infrastructure_failed` | A unique temporary artifact name could not be produced | Check that the state root is writable and the disk has free space, then retry |
| `artifact_temp_write_failed` | `infrastructure_failed` | Writing the temporary artifact file failed | Check that the state root is writable and the disk has free space, then retry |
| `artifact_unowned_final_collision` | `infrastructure_failed` | A file this server does not own already occupies the artifact path {path} | Move or remove that file, then start a new run |
| `artifact_winner_alias_failed` | `infrastructure_failed` | The winner was selected but its patch alias could not be written, so no representative patch path exists | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_winner_alias_not_selected` | `infrastructure_failed` | A winner alias was requested for a run whose selection names no candidate | Retry the run; if it recurs, report the reasonCode and the run log |
| `artifact_winner_candidate_mismatch` | `infrastructure_failed` | The candidate patch on disk no longer matches the reference the run manifest recorded | Start a new run; the state this run built can no longer be trusted |
| `artifact_winner_candidate_unavailable` | `infrastructure_failed` | The selected candidate has no stored patch to point the winner alias at | Start a new run; the state this run built can no longer be trusted |
| `artifact_write_failed` | `infrastructure_failed` | A guarded artifact write was refused for a reason this server does not classify yet | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `artifact_write_interrupted` | `infrastructure_failed` | The artifact write was cut short, so its effect on disk is unknown | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `auth_login_required` | `provider_failed` | The {vendor} CLI is installed but not logged in for this run | Log in to the {vendor} CLI, then retry the run |
| `config_allowed_tools_missing` | `provider_failed` | A write role was built with no allowed-tools list, so the call was refused | Retry the run; if it recurs, report the reasonCode and the run log |
| `config_argument_above_maximum` | `blocked` | The argument {name} was {value}, above the largest value this tool accepts ({max}) | Pass {max} or less for {name}, then call the tool again |
| `config_argument_below_minimum` | `blocked` | The argument {name} was {value}, below the smallest value this tool accepts ({min}) | Pass {min} or more for {name}, then call the tool again |
| `config_argument_item_type_invalid` | `blocked` | Item {index} of the argument {name} is not {expected} | Make every item of {name} {expected}, then call the tool again |
| `config_argument_misspelled` | `blocked` | {name} is not an argument this tool accepts; the closest one is {suggestion} | Call the tool again with {suggestion}, or with only these arguments: {allowed} |
| `config_argument_not_finite` | `blocked` | The argument {name} was {value}, which is not a finite number | Pass a finite number for {name}, then call the tool again |
| `config_argument_not_in_enum` | `blocked` | The argument {name} was {value}, which is not one of the values this tool accepts | Pass one of these for {name}: {allowed} |
| `config_argument_not_integer` | `blocked` | The argument {name} was {value}, and this tool takes a whole number there | Pass a whole number for {name}, then call the tool again |
| `config_argument_required_missing` | `blocked` | The argument {name} is required and was not given | Call the tool again and include {name} |
| `config_argument_type_invalid` | `blocked` | The argument {name} must be {expected} | Pass {expected} for {name}, then call the tool again |
| `config_argument_unknown` | `blocked` | {name} is not an argument this tool accepts | Call the tool again with only these arguments: {allowed} |
| `config_argument_unsafe` | `provider_failed` | A model, effort or working-directory value looked like a command-line flag | Retry the run; if it recurs, report the reasonCode and the run log |
| `config_arguments_invalid` | `blocked` | The call arguments were not a plain JSON object whose properties are plain values | Call again with a plain JSON object |
| `config_arguments_not_accepted` | `blocked` | {name} was given, but this tool takes no arguments | Call the tool again with no arguments |
| `config_budget_invalid` | `blocked` | The step budget {budget} is outside the range of 1 to {limit} steps | Ask for a whole number of steps between 1 and {limit} |
| `config_candidate_count_invalid` | `blocked` | The candidate count {count} is neither one nor two | Ask for one or two candidates |
| `config_change_scope_missing` | `blocked` | A model or effort was given without the vendor and tier they belong to | Pass vendor and tier as well; the vendors are {vendors} and the tiers are {tiers} |
| `config_change_target_missing` | `blocked` | A vendor and tier were given with no model and no effort, so there is nothing to change | Pass model or effort together with them, or call the tool with no arguments to read the current settings |
| `config_effort_unsupported` | `blocked` | The model {model} does not offer the effort {effort} | Choose one of the efforts {model} offers ({efforts}), or leave effort empty to use the CLI default |
| `config_flag_banned` | `provider_failed` | The built command line carried a flag this server refuses to pass | Retry the run; if it recurs, report the reasonCode and the run log |
| `config_invalid` | `blocked` | The project configuration file {file} could not be validated: {detail} | Fix {file} so it matches the project configuration schema, commit it, then retry the run |
| `config_isolation_unsupported` | `blocked` | The isolation mode {isolation} is not one this server implements | Leave the isolation argument unset; the disposable worktree is the only mode |
| `config_permission_mode_invalid` | `provider_failed` | The permission mode this server builds for the delegate call was missing or empty | Retry the run; if it recurs, report the reasonCode and the run log |
| `config_project_path_invalid` | `blocked` | The project path {path} is not an absolute path | Pass the absolute path of the target git repository |
| `config_provider_unknown` | `blocked` | The {role} role was pinned to {vendor}, which is not a vendor this server registers | Use one of these vendors instead: {vendors} |
| `config_role_override_conflict` | `blocked` | Two candidates run a fixed mirrored placement, so a global worker or verifier pin cannot be honored | Drop the worker and verifier pins, or ask for one candidate |
| `config_role_unknown` | `provider_failed` | The delegate role {role} is not one this server can build a command line for | Retry the run; if it recurs, report the reasonCode and the run log |
| `config_schema_newer` | `blocked` | The project configuration file {file} declares schemaVersion {version}, and this server implements schemaVersion {supported} | Update this plugin to a version that reads schemaVersion {version}, or set {file} back to schemaVersion {supported}, then retry the run |
| `config_settings_effort_unsupported` | `blocked` | The {vendor} {tier} model {model} does not offer the effort {effort} | Choose one of the efforts {model} offers ({efforts}), or leave effort empty to use the CLI default |
| `config_settings_key_unknown` | `blocked` | {key} is not a key the settings section for {vendor} defines | Use one of these keys instead: {keys} |
| `config_settings_lock_unavailable` | `blocked` | The settings lock could not be taken: {detail} | Another process may be writing the settings; retry in a moment, the settings file was not changed |
| `config_settings_patch_empty` | `blocked` | The settings change carried no value to write | Call orch_config with no arguments to see the vendors, tiers and models you can set |
| `config_settings_patch_invalid` | `blocked` | The settings change was not an object | Call orch_config with no arguments to see the vendors, tiers and models you can set |
| `config_settings_read_failed` | `blocked` | The existing settings file could not be read: {detail} | Check the file permissions and contents, then retry; the existing settings file was left unchanged |
| `config_settings_section_invalid` | `blocked` | The settings given for the vendor {vendor} were not an object | Call orch_config with no arguments to see the vendors, tiers and models you can set |
| `config_settings_value_not_string` | `blocked` | The {key} value for the vendor {vendor} was not text | Pass text for {key}, or an empty string to clear it |
| `config_settings_value_unsafe` | `blocked` | The {key} value for the vendor {vendor} contains a line break or a zero byte | Pass {key} as a single line with no zero bytes |
| `config_settings_vendor_unknown` | `blocked` | {vendor} has no section in the settings file | Use one of these vendors instead: {vendors} |
| `config_settings_write_failed` | `blocked` | The settings file could not be written: {detail} | Check the contents of the settings file and the permissions and free space on that path; this was the write itself failing, not a race |
| `config_single_vendor_conflict` | `blocked` | Two candidates and the single-vendor allowance cannot be asked for in the same call | Ask for two candidates without the single-vendor allowance, or ask for one candidate |
| `config_task_missing` | `blocked` | The task text was empty, so there was nothing to delegate | Describe what to do in at least one sentence |
| `config_tests_cwd_unusable` | `blocked` | The tests.cwd directory named in the project configuration does not exist inside the isolated worktree, or leads out of it | Fix tests.cwd in .bom-orch.json so it names a directory that the repository actually carries, commit it, then retry the run |
| `config_tool_pattern_unsafe` | `provider_failed` | An allowed-tools entry looked like a command-line flag | Retry the run; if it recurs, report the reasonCode and the run log |
| `config_tool_set_missing` | `provider_failed` | The role needs an explicit tool set and none was given | Retry the run; if it recurs, report the reasonCode and the run log |
| `config_tool_unknown` | `blocked` | {name} is not a tool this server serves | Call one of these instead: {tools} |
| `config_uncommitted` | `blocked` | The project configuration file {file} has an uncommitted change, so the settings this run read are not the ones in the working tree | Commit or stash the change to {file}, then retry the run |
| `config_unreadable` | `blocked` | The project configuration file {file} could not be read from the repository: {detail} | Retry the run; if it fails again, check that git can run in this repository |
| `config_wait_ms_invalid` | `blocked` | The wait budget {waitMs} is not a finite, non-negative number of milliseconds | Pass the wait budget in whole milliseconds, or leave it unset |
| `deps_unavailable` | `blocked` | Dependency provisioning is switched on in {file}, but the dependencies could not be installed: {detail} | Commit a lockfile that matches package.json if the baseline carries none, or make the package registry reachable and retry if the install itself failed, or set tests.provisionDeps to "none" in {file} to run without it |
| `evidence_adapter_incomplete` | `infrastructure_failed` | The test reporter produced no readable events, so this run carries no machine evidence | Check that the project test command runs a supported reporter, then retry |
| `evidence_artifact_too_large` | `infrastructure_failed` | The evidence record exceeded the artifact size limit of {limit} bytes | Narrow the test scope so the evidence stays under {limit} bytes, then retry |
| `evidence_authority_mismatch` | `infrastructure_failed` | The stored evidence did not match the sealed attempt it claims to describe, so it carries no authority | Start a new run; the state this run built can no longer be trusted |
| `evidence_cleanup_unproven` | `infrastructure_failed` | An evidence worktree could not be proven removed, and handing it to the reaper failed | Remove the leftover worktree under the state root, then retry |
| `evidence_controller_authority_lost` | `infrastructure_failed` | The evidence controller hard-stopped, so the artifact store it owned can no longer be trusted | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `evidence_persistence_failed` | `infrastructure_failed` | An evidence artifact could not be written durably during the proof run | Check that the state root is writable and the disk has free space, then retry |
| `evidence_revision_mismatch` | `infrastructure_failed` | The evidence worktree sits on a different commit than the proof asked for | Start a new run; the state this run built can no longer be trusted |
| `evidence_seal_failed` | `infrastructure_failed` | The revision identity did not match the snapshot it claims to seal, so the attempt could not be sealed | Retry the run; if it recurs, report the reasonCode and the run log |
| `evidence_snapshot_failed` | `infrastructure_failed` | The snapshot that seals a writer attempt produced no commit id, so there is nothing to build evidence on | Check that the target path is a healthy git repository, then retry |
| `evidence_spec_invalid` | `infrastructure_failed` | The evidence run was requested with a specification that failed validation | Retry the run; if it recurs, report the reasonCode and the run log |
| `evidence_store_authority_lost` | `infrastructure_failed` | The evidence writer lost the artifact store authority it was given, so the store can no longer be trusted | Start a new run; if it recurs, check that the state root is intact and report the reasonCode |
| `evidence_unavailable` | `unverified` | Machine evidence or the regression proof could not be obtained at all, so the candidate is only usable unverified | Read the candidate diff yourself before applying it, or retry the run |
| `evidence_unstable` | `infrastructure_failed` | Two repetitions of the same test group disagreed, so the evidence is flaky | Stabilize the flaky test, then retry the run |
| `evidence_worktree_invalid` | `infrastructure_failed` | The evidence worktree failed its shape check, so no test could run in it | Retry the run; if it recurs, report the reasonCode and the run log |
| `evidence_worktree_unavailable` | `infrastructure_failed` | The evidence worktree for the proof run could not be created | Check that the state root is writable and the disk has free space, then retry |
| `git_argument_count_exceeded` | `infrastructure_failed` | A git command was assembled with more than {limit} arguments, so it was refused before the spawn | Retry the run; if it recurs, report the reasonCode and the run log |
| `git_arguments_invalid` | `infrastructure_failed` | A Git invocation request was refused before spawn because {detail} | Pass arguments as a plain array of strings and optional stdin as a finite bounded Buffer, then retry the run |
| `git_cli_unavailable` | `blocked` | The git executable could not be run, so the project repository could not be inspected | Install git and check that PATH reaches it, then retry the run |
| `git_command_failed` | `infrastructure_failed` | A git command exited with a non-zero status and wrote nothing this server could report | Check that the target path is a healthy git repository, then retry |
| `git_diff_failed` | `infrastructure_failed` | The revision delta listing failed, or it returned entries that are not a valid sorted diff | Check that the target path is a healthy git repository, then retry |
| `git_environment_invalid` | `infrastructure_failed` | A git command was assembled with an environment that is not a plain object of string values, so it was refused before the spawn | Retry the run; if it recurs, report the reasonCode and the run log |
| `git_environment_value_invalid` | `infrastructure_failed` | The git environment entry {name} is not a string, so the command was refused before the spawn | Retry the run; if it recurs, report the reasonCode and the run log |
| `git_global_option_refused` | `infrastructure_failed` | The git command carried the leading global option {option}, which is not on the hardening allow list | Retry the run; if it recurs, report the reasonCode and the run log |
| `git_head_unborn` | `blocked` | The project repository has no commit for HEAD to point at, so no worktree can be created | Make at least one commit in the project, or run against a repository that already has one, then retry |
| `git_invocation_failed` | `infrastructure_failed` | Reading the call arguments or starting git raised a fault, so the command was refused rather than run unscreened | Retry the run; if it recurs, report the reasonCode and the run log |
| `git_process_unkillable` | `infrastructure_failed` | The git process did not exit after the deadline kill, so the child may still be running | Check the host for a git process left behind, then retry the run |
| `git_project_path_missing` | `blocked` | No project path was given, so there is no repository to inspect | Pass the absolute path of the target git repository |
| `git_project_root_not_canonical` | `blocked` | The project path {path} could not be resolved to a canonical directory | Give the absolute path of an existing git repository, then retry |
| `git_repository_missing` | `blocked` | The project path {path} exists but is not inside a git repository | Give the path of a git repository as the project, then retry the run |
| `git_seal_failed` | `infrastructure_failed` | A writer attempt could not be sealed: the snapshot, the revision delta or the collected patch did not verify | Retry the run; if it recurs, report the reasonCode and the run log |
| `git_snapshot_failed` | `infrastructure_failed` | The snapshot of the writer step produced no commit id, so the attempt could not be sealed | Check that the target path is a healthy git repository, then retry |
| `git_spawn_failed` | `infrastructure_failed` | The git process never started, so the command produced no exit status | Install git and check that PATH reaches it, then retry the run |
| `git_version_below_floor` | `blocked` | The installed git {version} is older than {floor}, the first release that stops following out-of-tree symlinks during checkout on case-insensitive filesystems (CVE-2024-32002) | Upgrade git to {floor} or later, then retry the run |
| `git_working_directory_invalid` | `infrastructure_failed` | A git command was assembled with a working directory that is not an absolute path, so it was refused before the spawn | Retry the run; if it recurs, report the reasonCode and the run log |
| `judge_cancelled` | `cancelled` | The host cancelled this run before the blind judge could finish | Start a new run when you want the work again |
| `judge_deadline` | `deadline_exceeded` | The run deadline passed before the blind judge could finish | Raise wait_ms and retry the run |
| `judge_decision_invalid` | `provider_failed` | The judge decision named neither a tie nor a known candidate lane | Retry the run; if the judge keeps answering unusably, report the reasonCode and the run log |
| `judge_format_invalid` | `provider_failed` | The judge answer was empty, oversized, malformed, or could not be matched back to the blinded lanes | Retry the run; if the judge keeps answering unusably, report the reasonCode and the run log |
| `judge_invalid` | `provider_failed` | Neither blind judge produced a decision this server could use, so no candidate was selected | Retry the run; if the judge keeps answering unusably, report the reasonCode and the run log |
| `judge_json_invalid` | `provider_failed` | The judge answer was not valid JSON | Retry the run; if the judge keeps answering unusably, report the reasonCode and the run log |
| `judge_nonce_preparation_failed` | `infrastructure_failed` | The blind judge identity could not be prepared, so judging never ran | Retry the run; if it recurs, report the reasonCode and the run log |
| `judge_provider_failure` | `provider_failed` | The judge call to the {vendor} CLI failed, was truncated, or ran out of time | Read the labeled vendor stderr excerpt in the result, then retry the run |
| `judge_schema_invalid` | `provider_failed` | The judge answer did not match the verdict shape this server requires | Retry the run; if the judge keeps answering unusably, report the reasonCode and the run log |
| `judge_scratch_failed` | `infrastructure_failed` | The judge scratch worktree could not be prepared or cleaned, so the decision is invalid | Check that the state root is writable and the disk has free space, then retry |
| `judge_tie` | `tie` | The blind judges disagreed, reported a tie, or found major defects, so no candidate was selected | Read both candidate patches and choose one yourself, or retry the run |
| `judge_view_unavailable` | `infrastructure_failed` | A blind judge pair could not be built from the candidate summaries, so judging never ran | Retry the run; if it recurs, report the reasonCode and the run log |
| `lane_budget_exhausted` | `budget_exhausted` | The lane used every attempt in its budget without reaching a decision | Raise the step budget or narrow the task, then retry |
| `lane_stagnated` | `rejected` | Two attempts in a row produced the same tree and the same open issues, so the lane stopped | Narrow the task or split it, then retry |
| `lane_unverified` | `unverified` | The verifier verdict could not be read even after the format-correction retry | Read the candidate diff yourself before applying it, or retry the run |
| `lane_verified` | `verified` | Every gate passed: the verifier approved, no issues are open, and the machine evidence and regression proof are green | Review the patch and apply it if you agree |
| `learning_applied_axes_missing` | `blocked` | This run record does not say which cells its grade was applied to, so that contribution cannot be taken back | Clear the learning state with a reset and let it fill again; that is the recovery path for records written in the old format |
| `learning_applied_grade_unknown` | `blocked` | This run record claims the grade {grade}, which cannot be turned back into a contribution | Adding a new grade without taking the old one back would count it twice; read the journal row before correcting it again |
| `learning_arm_missing` | `blocked` | A learning update named no arm | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_cell_key_conflict` | `blocked` | A reset named both a single cell and a list of cells | Name either one cell or a list of cells, not both |
| `learning_cell_key_duplicated` | `blocked` | The cell {cell} came twice in one transaction, and one run makes at most one observation per axis | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_cell_key_invalid` | `blocked` | The cell name was empty or was not text | Name the cell as text, or leave it out to clear everything |
| `learning_cell_key_missing` | `blocked` | A learning update named no cell | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_cell_keys_invalid` | `blocked` | The list of cells to clear was not a list of non-empty names | Pass a list of cell names, or leave it out to clear everything |
| `learning_choice_map_invalid` | `blocked` | This run record was written by policy version 2 but its recorded choices are not intact | Leave this journal row alone; start a new run to get a record in the current format |
| `learning_generation_expired` | `blocked` | A reset retired the learning generation this run belongs to, so its grade can no longer be corrected | Start a new run and correct that one instead |
| `learning_generations_read_failed` | `infrastructure_failed` | The learning generations file could not be read: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_generations_snapshot_failed` | `infrastructure_failed` | The generations snapshot could not be written, so nothing was reset: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_journal_read_failed` | `infrastructure_failed` | The run journal could not be read: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_journal_record_invalid` | `blocked` | The journal record was not an object carrying a run identity | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_journal_record_unserializable` | `blocked` | The journal record could not be turned into JSON: {detail} | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_journal_row_unbuildable` | `infrastructure_failed` | The replacement journal row could not be built: {detail} | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_journal_row_write_failed` | `infrastructure_failed` | The journal row that explains this learning change could not be appended: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_lock_unavailable` | `blocked` | The learning lock could not be taken: {detail} | Retry in a moment; pending learning work is replayed by the next read or retry |
| `learning_mutation_failed` | `infrastructure_failed` | The learning state and the run record could not be changed together: {detail} | Retry in a moment; pending learning work is replayed by the next read or retry |
| `learning_pending_work_invalid` | `infrastructure_failed` | The pending learning work is malformed, so it cannot be replayed: {detail} | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_pending_work_read_failed` | `infrastructure_failed` | The pending learning work could not be read: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_pending_work_unclearable` | `infrastructure_failed` | The pending learning work was replayed but its record could not be removed: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_policy_version_unknown` | `blocked` | This run record was written by learning policy version {version}, which this server cannot correct | Correct it with the version of this server that wrote the row; rolling it back with a different version counts the contribution twice |
| `learning_posteriors_not_json` | `infrastructure_failed` | The learning state file is not readable as JSON: {detail} | Clear the learning state with a reset; the unreadable bytes are kept beside it first |
| `learning_posteriors_quarantine_failed` | `infrastructure_failed` | The learning state could not be read and could not be set aside as {path} either: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_posteriors_read_failed` | `infrastructure_failed` | The learning state file could not be read: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_posteriors_reset_failed` | `infrastructure_failed` | The learning state could not be cleared: {detail} | Retry the same reset in a moment; pending work is replayed by the next read, and if it keeps failing stop every server on this state root and restore the paired snapshot |
| `learning_posteriors_shape_invalid` | `infrastructure_failed` | The learning state file is JSON but not a set of cells | Clear the learning state with a reset; the unusable bytes are kept beside it first |
| `learning_run_not_found` | `blocked` | No run named {runId} is in the journal | Ask orch_stats for the recent run list and use one of the identities it prints |
| `learning_run_task_class_missing` | `blocked` | The run {runId} has no task class, so there is no learning cell to correct | This run is not tied to a learning cell; choose a different run |
| `learning_scope_read_failed` | `infrastructure_failed` | The learning state could not be read, so the cells of {taskClass} alone could not be cleared: {detail} | Ask for a reset without a task class; that discards the unreadable file whole |
| `learning_scope_unreadable` | `infrastructure_failed` | A single task class cannot be picked out of a corrupt learning state | Ask for a reset without a task class; that discards the unreadable file whole |
| `learning_snapshot_failed` | `infrastructure_failed` | The snapshot could not be written, so nothing was reset: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_task_class_conflict` | `blocked` | A task class was named together with an explicit cell list | Name either a task class or a cell list, not both |
| `learning_task_class_invalid` | `blocked` | The task class was empty or was not text | Name the task class as text, or leave it out to clear everything |
| `learning_updates_invalid` | `blocked` | The learning updates were not a list | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_work_failed` | `infrastructure_failed` | The learning write failed | Retry in a moment; pending learning work is replayed by the next read or retry |
| `learning_work_invalid` | `blocked` | The learning work is missing its version, its operation identity or its learning target | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_work_journal_invalid` | `blocked` | The journal target of the learning work was not an object | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_work_journal_run_id_missing` | `blocked` | The journal target of the learning work carries no run identity | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_work_publish_failed` | `infrastructure_failed` | {name} could not be moved into place: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_work_quarantine_invalid` | `blocked` | The quarantine target of the learning work was not the corrupt learning state bytes | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_work_unserializable` | `infrastructure_failed` | {name} could not be turned into JSON: {detail} | Retry the run; if it recurs, report the reasonCode and the run log |
| `learning_work_write_failed` | `infrastructure_failed` | {name} could not be written: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `learning_write_boundary_failed` | `infrastructure_failed` | The learning write failed at the {name} boundary: {detail} | Retry in a moment; pending learning work is replayed by the next read or retry |
| `preflight_cross_vendor_unavailable` | `blocked` | Cross-vendor verification needs two vendor CLIs and only one is available | Install both vendor CLIs, or pass the single-vendor allowance to accept a one-vendor run |
| `preflight_gateway_env_unsupported` | `blocked` | This host selects a vendor gateway deployment ({names}), which this server does not support | Unset {names} in the shell that starts this server, or start it from a shell without them, then retry the run |
| `preflight_no_provider_available` | `blocked` | No vendor CLI answered the preflight, so no role could be filled | Check the installation and PATH, then retry the run |
| `preflight_provider_unavailable` | `blocked` | The {role} role was pinned to {vendor}, which the preflight reported unavailable | Install that vendor CLI and check that PATH reaches it, then retry the run |
| `provider_below_security_floor` | `blocked` | The {vendor} CLI at version {version} is below the security floor a write role requires | Update the {vendor} CLI to {floor} or newer, or pin a different vendor as the writer |
| `provider_cli_not_found` | `provider_failed` | The {vendor} CLI was not found on PATH | Install the {vendor} CLI and check that PATH reaches it, then retry |
| `provider_cli_shim_only` | `provider_failed` | Only a shell shim was found for the {vendor} CLI, and this server spawns without a shell | Install the native {vendor} executable, then retry |
| `provider_call_halted` | `provider_failed` | The {vendor} call was cut short before it produced a result | Check the run log or retry the run |
| `provider_deadline_exceeded` | `deadline_exceeded` | The {vendor} writer call ran out of time at the deadline boundary, or the lane deadline passed first | Raise wait_ms and retry the run |
| `provider_error_unclassified` | `provider_failed` | The {vendor} CLI failed in a way this server does not classify yet | Read the labeled vendor stderr excerpt in the result, then retry the run |
| `provider_exit_nonzero` | `provider_failed` | The {vendor} CLI exited with a non-zero status | Read the labeled vendor stderr excerpt in the result, then retry the run |
| `provider_no_terminal_record` | `provider_failed` | The {vendor} event stream ended without a final record, so the answer is incomplete | Read the labeled vendor stderr excerpt in the result, then retry the run |
| `provider_outcome_unknown` | `provider_failed` | It cannot be proven whether the writer call changed the worktree, so that worktree was quarantined | Start a new run; the quarantined worktree is left for the reaper |
| `provider_output_truncated` | `provider_failed` | The {vendor} CLI stopped at its own output or turn limit before finishing the answer | Narrow the task so the answer fits, then retry |
| `provider_rate_limited` | `provider_failed` | The {vendor} CLI refused the call because the account usage limit is exhausted | Wait for the {vendor} usage limit to reset, or run with the other vendor |
| `provider_reported_failure` | `provider_failed` | The {vendor} CLI reported that the writer step failed | Read the labeled vendor stderr excerpt in the result, then retry the run |
| `provider_signal_killed` | `provider_failed` | The {vendor} process was killed by a signal before it produced a result | Retry the run; if it recurs, check the host for memory pressure or an external process reaper |
| `provider_spawn_denied` | `provider_failed` | The operating system refused to execute the {vendor} CLI at {path} | Install the native {vendor} executable, or fix the execute permission on that file, then retry |
| `provider_spawn_failed` | `provider_failed` | The {vendor} executable disappeared between the PATH lookup and the spawn | Install the {vendor} CLI and check that PATH reaches it, then retry |
| `provider_stream_unparsable` | `provider_failed` | The {vendor} event stream carried records this server could not read, so the result is incomplete | Retry the run; if it recurs, report the reasonCode and the {vendor} CLI version |
| `provider_turn_failed` | `provider_failed` | The {vendor} CLI reported that the turn itself failed | Check the run log or retry the run |
| `resume_baseline_mismatch` | `blocked` | The run named {runId} was built on a different baseline than this call prepared, so its sealed attempts describe a different source tree | Call orch_run again without resume_run_id to start a fresh run |
| `resume_environment_mismatch` | `blocked` | The run named {runId} froze its test plan in a different environment, so the evidence it sealed would not be evidence here | Call orch_run again without resume_run_id to start a fresh run |
| `resume_manifest_unreadable` | `blocked` | The run named {runId} is on this state root but what it left could not be read | Call orch_run again without resume_run_id to start a fresh run |
| `resume_run_not_found` | `blocked` | No run named {runId} is on this state root, so there is nothing to resume | Call orch_status with no arguments to list the recent runs |
| `run_all_candidates_blocked` | `blocked` | Every candidate lane was blocked, so no patch was produced | Read the per-candidate reasons in the result, fix what they name, then retry |
| `run_all_candidates_rejected` | `rejected` | Every candidate was rejected, so there was nothing eligible to select | Read the per-candidate reasons in the result, fix what they name, then retry |
| `run_binding_invalid` | `blocked` | The planner, worker and verifier could not be bound to distinct available vendors | Install both vendor CLIs, or pass allow_single to accept a single-vendor run |
| `run_binding_preparation_failed` | `blocked` | Assigning the vendor roles for this run failed before any call was made | Retry the run; if it recurs, report the reasonCode and the run log |
| `run_cancelled` | `cancelled` | The host cancelled this run before it finished | Start a new run when you want the work again |
| `run_deadline_exceeded` | `deadline_exceeded` | The {waitMs} millisecond deadline for this run passed before it finished | Raise wait_ms and retry the run |
| `run_deadline_unrepresentable` | `blocked` | The clock and the wait budget do not produce a deadline this server can represent exactly | Check the system clock and the wait budget, then retry |
| `run_nested_invocation` | `blocked` | This server is running inside a delegate that an orchestration started (run {runId}), so it refuses every tool call | You are already inside an orchestration; run the tool from the outer process instead |
| `run_orchestration_failed` | `infrastructure_failed` | The orchestration stopped on an unexpected internal fault | Retry the run; if a worktree is left behind, check the worktrees directory under the state root |
| `run_preparation_input_invalid` | `blocked` | The run preparation was entered with input that had already failed validation | Retry the run; if it recurs, report the reasonCode and the run log |
| `run_result_unserializable` | `infrastructure_failed` | The result could not be turned into JSON | The result may be too large or hold a cycle; retry with a narrower request |
| `run_selection_unavailable` | `no_candidate` | The candidates ended in a mix of outcomes that no selection rule covers | Read the per-candidate reasons in the result, fix what they name, then retry |
| `run_tool_failed` | `infrastructure_failed` | The tool call failed: {detail} | Retry the call or read the server log |
| `run_tool_handler_missing` | `infrastructure_failed` | The tool {name} is published but this server has no handler wired for it | This is not something a caller can fix; report this sentence together with the server log |
| `scope_files_input_invalid` | `infrastructure_failed` | The changed-file list was not a list of strings, so the patch scope could not be checked | Retry the run; if it recurs, report the reasonCode and the run log |
| `scope_index_unreadable` | `infrastructure_failed` | The worktree index could not be read, so the symbolic links the patch touches could not be resolved | Retry the run; the patch this attempt produced cannot be trusted |
| `scope_inspection_failed` | `infrastructure_failed` | The patch scope check stopped on an unexpected internal fault | Retry the run; if it recurs, report the reasonCode and the run log |
| `scope_path_policy_unavailable` | `policy_failure` | The patch scope policy could not run because the ignored and unsafe path lists were missing | Retry the run; if it recurs, report the reasonCode and the run log |
| `scope_policy_failure` | `policy_failure` | The patch touched a path the scope policy forbids, or it changed what the tests are | Read the flagged paths in the result, then ask for a change that stays inside the allowed paths |
| `scope_tamper_failure` | `policy_failure` | A tamper signal was raised for this attempt, so its decision cannot be trusted | Retry with a narrower task; if it recurs, report the reasonCode and the run log |
| `scope_worktree_path_missing` | `infrastructure_failed` | The worktree path was missing, so the symbolic links the patch touches could not be checked | Retry the run; if it recurs, report the reasonCode and the run log |
| `state_directory_create_failed` | `blocked` | The state directory could not be created: {detail} | Check the permissions on that path, then retry |
| `state_lock_create_failed` | `blocked` | The lock file could not be created: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `state_lock_path_invalid` | `blocked` | The lock was asked for with an empty path | Retry the run; if it recurs, report the reasonCode and the run log |
| `state_lock_timeout` | `blocked` | The lock was still held when the wait budget ran out (last error: {code}) | Retry in a moment; if it never clears, check the permissions on the state root |
| `state_lock_token_write_failed` | `blocked` | The ownership marker could not be written, so the work under the lock never started: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `state_lock_work_failed` | `blocked` | The work under the state-root lock failed after the lock was taken: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `state_lock_work_not_callable` | `blocked` | The work to run under the lock was not a function | Retry the run; if it recurs, report the reasonCode and the run log |
| `state_recovery_intent_unavailable` | `blocked` | The durable recovery intent could not be recorded, so the worktree effect was not started | Check that the state root is writable and has free space, then retry |
| `state_root_not_absolute` | `blocked` | The state root is not an absolute path | Set BOM_ORCH_HOME to an absolute path or leave it empty |
| `state_schema_newer` | `blocked` | The write to shared state file {file} was blocked because it declares schemaVersion {version}, and this server implements schemaVersion {supported} | Update this plugin to a version that reads schemaVersion {version}, then retry without changing or downgrading {file} |
| `status_run_not_found` | `blocked` | No run named {runId} is on this state root | Call orch_status with no arguments to list the recent runs |
| `status_run_unreadable` | `infrastructure_failed` | The run records on this state root could not be read | Call orch_status with no arguments to list the recent runs |
| `test_abort_signal_invalid` | `infrastructure_failed` | The frozen test run was handed something that is not an abort signal | Retry the run; if it recurs, report the reasonCode and the run log |
| `test_aborted_before_start` | `infrastructure_failed` | The run was already cancelled when the test was asked to start, so nothing ran | Start a new run when you want the work again; if it keeps stopping early, raise wait_ms |
| `test_baseline_not_green` | `rejected` | The baseline test run did not pass, so a later failure would prove nothing | Make the project test suite pass on the baseline commit, then retry |
| `test_cache_entry_invalid` | `infrastructure_failed` | The evidence cache held a record this server did not write, so the whole cache was refused | Start a new run; the state this run built can no longer be trusted |
| `test_candidate_failed` | `rejected` | The candidate test run did not pass | Read the failing tests in the result, then retry with a narrower task |
| `test_candidate_witness_missing` | `rejected` | The candidate test run did not observe the exact failures the regression test identified | Read the failing tests in the result, then retry with a narrower task |
| `test_command_unavailable` | `infrastructure_failed` | The frozen test plan has no runnable command in this environment | Install the project test tool and check that PATH reaches it, then retry |
| `test_deadline_expired` | `deadline_exceeded` | The run deadline passed inside the test queue, so no further test could run | Raise wait_ms and retry the run |
| `test_definition_tampering` | `policy_failure` | The delegate changed a file that defines what the tests are | Ask for a change that leaves the test configuration alone, then retry |
| `test_delta_apply_failed` | `infrastructure_failed` | Applying the test-only part of the patch to the evidence worktree failed | Retry the run; if it recurs, report the reasonCode and the run log |
| `test_delta_authority_mismatch` | `infrastructure_failed` | The test-only patch did not verify against its own bytes, digest and path list | Start a new run; the state this run built can no longer be trusted |
| `test_delta_collection_failed` | `infrastructure_failed` | Collecting the test-only part of the patch from git failed | Check that the target path is a healthy git repository, then retry |
| `test_delta_collection_mismatch` | `infrastructure_failed` | The collected test-only patch did not list exactly the paths that were requested | Retry the run; if it recurs, report the reasonCode and the run log |
| `test_delta_deletion_only` | `rejected` | Every test change in the patch is a deletion | Ask for a change that adds a failing test instead of removing tests, then retry |
| `test_delta_duplicate_path` | `policy_failure` | The patch listed the path {path} twice | Retry the run; the patch this attempt produced cannot be trusted |
| `test_delta_empty` | `rejected` | The candidate changed no test files at all | Ask for a change that adds a test proving the fix, then retry |
| `test_delta_ignored_path` | `policy_failure` | The patch touched {path}, which this repository tells git to ignore | Ask for a change that stays inside tracked paths, then retry |
| `test_delta_ignored_path_invalid` | `policy_failure` | An ignored path in the patch failed the strict relative-path check | Retry the run; the patch this attempt produced cannot be trusted |
| `test_delta_input_invalid` | `infrastructure_failed` | The test-only split was given an entry list or a revision it could not read | Retry the run; if it recurs, report the reasonCode and the run log |
| `test_delta_path_invalid` | `policy_failure` | A path in the patch failed the strict relative-path check | Retry the run; the patch this attempt produced cannot be trusted |
| `test_delta_unavailable` | `infrastructure_failed` | The patch carried no test-only part that could be separated from the rest | Retry the run; the patch this attempt produced cannot be trusted |
| `test_delta_unsafe_path` | `policy_failure` | The patch touched a path git flagged unsafe, or a file mode this server refuses to apply | Retry the run; the patch this attempt produced cannot be trusted |
| `test_delta_unsafe_path_invalid` | `policy_failure` | An unsafe path in the patch failed the strict relative-path check | Retry the run; the patch this attempt produced cannot be trusted |
| `test_dependency_unavailable` | `infrastructure_failed` | The test run failed because the project dependencies are not installed | Install the project dependencies, then retry |
| `test_environment_drift` | `infrastructure_failed` | The runtime environment no longer matches the frozen test plan | Retry the run so the plan is frozen against the current environment |
| `test_event_file_cleanup_unproven` | `infrastructure_failed` | The reporter event file could not be proven removed, so its bytes are not trusted as evidence | Check that the state root is writable and the disk has free space, then retry |
| `test_event_file_creation_unproven` | `infrastructure_failed` | The reporter event file could not be proven to be the file this server just created | Check that the state root is writable and the disk has free space, then retry |
| `test_event_file_identity_drift` | `infrastructure_failed` | The reporter event file is no longer the file this server created before the test spawned | Retry the run; if it recurs, check for another process writing under the state root |
| `test_executable_drift` | `infrastructure_failed` | The test executable changed since the test plan was frozen | Retry the run so the plan is frozen against the current executable |
| `test_execution_unavailable` | `infrastructure_failed` | The test run never completed, so it produced no result to read | Retry the run; if it recurs, check whether the project test command hangs on this host |
| `test_frozen_execution_failed` | `infrastructure_failed` | The test run failed before it could report an exit code ({detail}) | Retry the run; if it recurs, report the reasonCode and the run log |
| `test_helper_untrusted` | `infrastructure_failed` | The patch touched a test file the reporter does not trust as an ordinary test | Read the candidate diff yourself before applying it, or retry the run |
| `test_launcher_drift` | `infrastructure_failed` | A launcher in the test command no longer resolves to the executable the plan froze | Retry the run so the plan is frozen against the current executable |
| `test_machine_failed` | `infrastructure_failed` | The tests report failure and there is no budget left to repair it | Read the failing tests in the result, then retry with a bigger step budget |
| `test_pinned_definition_drift` | `infrastructure_failed` | {file} defines what the tests are, and its {key} changed after the plan was frozen | Leave the test configuration unchanged while a run is in flight, then retry |
| `test_plan_invalid` | `infrastructure_failed` | The frozen test plan failed validation, so no test could be started | Retry the run; if it recurs, report the reasonCode and the run log |
| `test_plan_untrusted` | `infrastructure_failed` | The project test command is not one of the reporters this server trusts as a regression witness | Run the project tests through a supported reporter, or accept the candidate as unverified |
| `test_process_cleanup_unproven` | `infrastructure_failed` | Test child processes could not be proven dead, so no further test could run in this run | Kill the leftover test processes, then retry |
| `test_proof_not_proven` | `rejected` | The regression proof is repairable but unproven, and there is no budget left | Raise the step budget and retry the run |
| `test_queue_poisoned` | `infrastructure_failed` | An earlier fault poisoned the test queue, so no further test could run in this run | Start a new run; the state this run built can no longer be trusted |
| `test_record_invalid` | `infrastructure_failed` | A test record failed its shape check, so it cannot be used as evidence | Retry the run; if it recurs, report the reasonCode and the run log |
| `test_regression_collection_failed` | `rejected` | The regression test failed while collecting tests instead of at an assertion | Read the failing tests in the result, then retry with a narrower task |
| `test_regression_did_not_fail` | `rejected` | The baseline plus the new test passed, so the new test does not reproduce the bug | Describe the failure you want reproduced more precisely, then retry |
| `test_regression_witness_missing` | `rejected` | The regression test failed without the reproduced assertion this server needs as a witness | Read the failing tests in the result, then retry with a narrower task |
| `test_start_invalid` | `infrastructure_failed` | The test queue was handed something other than a test to start | Retry the run; if it recurs, report the reasonCode and the run log |
| `verifier_failed` | `provider_failed` | The verifier returned a structured FAIL verdict and there was no budget left to repair it | Read the verifier issues in the result, then retry with a bigger step budget |
| `verifier_issue_limit_exceeded` | `rejected` | The issue ledger would exceed the {limit} blocking-issue limit | Narrow the task so fewer issues are raised, then retry |
| `verifier_issues_open` | `rejected` | Blocking issues are still open after the verdict and the budget is exhausted | Read the open issues in the result, then retry with a bigger step budget |
| `verifier_mutation` | `policy_failure` | The read-only verifier changed the worktree, so its verdict cannot be trusted | Retry the run; if it recurs, run with the other vendor as the verifier |
| `verifier_operational_failure` | `provider_failed` | The verifier call itself failed, or its mutation check could not be taken at all | Read the labeled vendor stderr excerpt in the result, then retry the run |
| `verifier_verdict_invalid` | `unverified` | The verifier verdict could not be read: {detail} | Read the candidate diff yourself before applying it, or retry the run |
| `worktree_add_failed` | `infrastructure_failed` | The disposable worktree could not be created: {detail} | Check that the target is a git repository with at least one commit, then remove any leftover worktree of the same name under the state root and retry |
| `worktree_baseline_missing` | `blocked` | The source worktree handle carries no shared baseline commit, so no revision worktree could be derived from it | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_creation_crashed` | `infrastructure_failed` | Creating the worktree stopped on an unexpected fault: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_creation_failed` | `infrastructure_failed` | The worktree for this step could not be created and no reason was reported | Check that the state root is writable and the disk has free space, then retry |
| `worktree_delta_listing_failed` | `infrastructure_failed` | The revision delta could not be listed: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_delta_mode_inconsistent` | `policy_failure` | The delta entry for {path} pairs a status with file modes that cannot both be true | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_delta_output_invalid` | `infrastructure_failed` | The raw revision delta from git was truncated, unterminated, or not in the record format this server reads | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_delta_path_unexpected` | `policy_failure` | git returned the delta path {path}, which is not literally one of the requested paths | Ask for the exact repository paths git returns, not case or normalization aliases |
| `worktree_delta_type_change` | `policy_failure` | The delta entry for {path} is a file type change, which a path-only delta cannot carry safely | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_delta_unsafe_mode` | `policy_failure` | The delta entry for {path} carries a symbolic link or nested repository mode, which is never used as apply authority | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_diff_output_missing` | `infrastructure_failed` | git reported a successful diff but left no patch file at {path} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_final_files_failed` | `infrastructure_failed` | The changed-file list of the final patch could not be read, so the change scope cannot be checked: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_final_patch_failed` | `infrastructure_failed` | The final patch could not be produced: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_final_patch_unreadable` | `infrastructure_failed` | The final patch bytes could not be read back: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_handle_invalid` | `blocked` | The worktree handle failed its shape check, so nothing was run in it | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_handle_path_unresolved` | `blocked` | The paths on the worktree handle could not be resolved to canonical paths inside the state root | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_head_unreadable` | `infrastructure_failed` | HEAD could not be read inside the worktree: {detail} | Check that the worktree still exists under the state root, then retry |
| `worktree_id_invalid` | `blocked` | The worktree id is not usable as a directory name under the state root | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_id_underivable` | `blocked` | No safe worktree directory name of at most 64 characters could be derived for this run | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_index_unlocatable` | `infrastructure_failed` | The worktree index could not be located at a canonical path that worktree owns | Start a new run; the state this run built can no longer be trusted |
| `worktree_not_pristine` | `blocked` | The revision worktree was not pristine, so the patch was not applied into it | Start a new run; the state this run built can no longer be trusted |
| `worktree_patch_apply_failed` | `infrastructure_failed` | The patch did not apply to the worktree, which was rolled back: {detail} | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_patch_bytes_invalid` | `blocked` | The patch to apply was not raw bytes | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_patch_delta_empty` | `blocked` | The patch produced no file delta at all, so there was nothing to apply | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_patch_digest_invalid` | `blocked` | The declared patch digest was not a full lowercase 64 character hex value | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_patch_digest_mismatch` | `blocked` | The patch bytes do not hash to the digest declared with them | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_patch_preflight_delta_unreadable` | `infrastructure_failed` | The path and mode preflight of the patch could not be read back: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_patch_preflight_index_failed` | `infrastructure_failed` | The temporary index for the patch preflight could not be built: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_patch_preflight_rejected` | `blocked` | The patch does not apply cleanly to the revision it names: {detail} | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_patch_preflight_tree_failed` | `infrastructure_failed` | The expected tree of the patch preflight could not be written: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_patch_processing_failed` | `infrastructure_failed` | Handling the patch bytes stopped on an unexpected fault: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_patch_tree_mismatch` | `infrastructure_failed` | The commit made from the applied patch does not carry exactly the tree the preflight produced | Check the filter and ignore settings of the target repository, then retry |
| `worktree_path_duplicate` | `policy_failure` | The path {path} appears twice in the list, either literally or under case and Unicode folding | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_path_in_use` | `blocked` | The worktree path {path} is registered to another run, so it was left untouched | Wait for the other run to finish, or prune that registration in the target repository, then retry |
| `worktree_path_list_invalid` | `infrastructure_failed` | The path list was not a list of at most {limit} non-empty strings | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_path_not_canonical` | `blocked` | The path {path} could not be resolved to a canonical path, so it was left untouched | Check that the path exists and is readable by this user, then retry |
| `worktree_path_outside_state_root` | `blocked` | The worktree path {path} resolves outside the worktrees directory of the state root | Check the worktrees directory under the state root for links that point elsewhere, then set BOM_ORCH_HOME to a clean absolute path and retry |
| `worktree_path_undecodable` | `policy_failure` | A path in the list carries a NUL byte or a character that did not decode as UTF-8, so it was refused rather than guessed | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_path_unsafe` | `policy_failure` | The path {path} is absolute, ambiguous across platforms, or able to escape the repository | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_path_windows_ambiguous` | `policy_failure` | The path {path} could resolve to a different file, an alternate data stream, or a device name on Windows | Retry the run; the patch this attempt produced cannot be trusted |
| `worktree_project_path_missing` | `blocked` | The worktree creation call carried no project path | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_purpose_invalid` | `blocked` | The worktree purpose is not one of the lane or evidence purposes this server allows | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_restore_unproven` | `infrastructure_failed` | A patch step failed and the worktree could not be proven restored, so it is quarantined | Start a new run; the state this run built can no longer be trusted |
| `worktree_revision_invalid` | `blocked` | The revision is not a full lowercase commit id | Use the full commit id git returned, then retry |
| `worktree_revision_not_commit` | `blocked` | The revision {revision} did not resolve to exactly that commit object: {detail} | Use a full lowercase commit id that exists in the repository, then retry |
| `worktree_revision_pair_invalid` | `blocked` | The from and to revisions are not both full lowercase object ids | Use the full object ids git returned, then retry |
| `worktree_revision_patch_failed` | `infrastructure_failed` | The patch between the two revisions could not be produced: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_revision_patch_unreadable` | `infrastructure_failed` | The revision patch bytes could not be read back: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_revision_tree_unresolved` | `infrastructure_failed` | The tree of the resolved commit could not be read: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_run_id_invalid` | `blocked` | The run id is not usable as a worktree directory name on every supported platform | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_scratch_cleanup_failed` | `infrastructure_failed` | The private patch scratch directory could not be cleaned up, so the applied result was rolled back | Clear the locked files under the scratch directory of the state root, then start a new run |
| `worktree_scratch_failed` | `infrastructure_failed` | The patch scratch directory could not be prepared or read: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_scratch_not_removed` | `infrastructure_failed` | The private patch scratch directory could not be proven removed, so its bytes may still be on disk | Check that the state root is writable and the disk has free space, then retry |
| `worktree_scratch_unusable` | `blocked` | The scratch directory under the state root could not be proven to be ours and inside the state root | Remove any link at the scratch directory under the state root, then retry |
| `worktree_shared_baseline_mismatch` | `blocked` | The second candidate worktree did not share the baseline commit of the first, so the two candidates would not be comparable | Start a new run; the state this run built can no longer be trusted |
| `worktree_snapshot_commit_failed` | `infrastructure_failed` | The worktree snapshot commit failed: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_source_baseline_mismatch` | `blocked` | The source worktree handle declares a baseline commit and tree that git does not resolve it to, so the handle itself is corrupt | Start a new run so the worktree handle is built fresh; nothing derived from this one can be trusted |
| `worktree_stage_failed` | `infrastructure_failed` | The worktree changes could not be staged: {detail} | Check the file permissions inside the worktree, then retry |
| `worktree_stale_registration_unreclaimed` | `blocked` | The dead worktree registration for {path} could not be reclaimed: {detail} | Prune the stale worktree registrations in the target repository, then retry |
| `worktree_state_root_inside_project` | `blocked` | The state root {path} is inside the target repository, so the worktrees would see each other and the final patch would carry the state root | Set BOM_ORCH_HOME to an absolute path outside the target repository, then retry |
| `worktree_state_root_missing` | `blocked` | The worktree creation call carried no state root path | Retry the run; if it recurs, report the reasonCode and the run log |
| `worktree_status_unreadable` | `infrastructure_failed` | The worktree status could not be read: {detail} | Check that the worktree still exists under the state root, then retry |
| `worktree_step_diff_failed` | `infrastructure_failed` | The diff of this step could not be produced: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_step_diff_unreadable` | `infrastructure_failed` | The diff bytes of this step could not be read back: {detail} | Check that the state root is writable and the disk has free space, then retry |
| `worktree_step_files_failed` | `infrastructure_failed` | The changed-file list of this step could not be read, so what the step touched cannot be checked: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_transplant_apply_failed` | `blocked` | The uncommitted changes could not be applied into the worktree, so the delegate would have edited stale code: {detail} | Commit or stash the local changes, then retry the run |
| `worktree_transplant_diff_failed` | `infrastructure_failed` | The patch of the uncommitted changes could not be produced: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_transplant_files_failed` | `infrastructure_failed` | The uncommitted changes were transplanted, but the list of which files they were could not be read, so the run cannot say what its baseline contains: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_transplant_index_failed` | `infrastructure_failed` | The temporary index for transplanting the uncommitted changes could not be built: {detail} | Check that the target path is a healthy git repository, then retry |
| `worktree_transplant_stage_failed` | `infrastructure_failed` | The uncommitted changes could not be staged for transplant: {detail} | Check the target repository for files this user cannot read, then retry |
